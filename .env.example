# Server
PORT=5500
FRONTEND_URL=http://localhost:5173

# Auth
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=synapse_sat
# Set to "true" when connecting to a managed Postgres that requires SSL.
DB_SSL=false

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
